// utils/interfaces.ts
export interface User {
    id: string;
    name: string;
    // Add any other properties relevant to your application
  }
  
  // utils/interfaces.ts
  // Existing code
  
  export interface jwtPayload {
    hash: string;
    user_id: string;
    // Add any other properties needed for your payload
  }
  